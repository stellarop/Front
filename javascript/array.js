
// 배열
// javascript 배열은 문자, 숫자, 객체, 함수 등도 포함 될 수 있다.

// length 배열 길이
// push 배열 끝에 값 추가
// pop 배열 끝에 값 삭제
// unshift 배열 앞 값 추가
// shift 배열 끝 값 삭제

let num = ['일', '이', '삼'];

num[0] = '수정'

num.push('사');

num.unshift('영');

console.log(num);
